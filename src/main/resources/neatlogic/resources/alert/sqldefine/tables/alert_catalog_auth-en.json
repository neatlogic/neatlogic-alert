{
  "moduleId": "alert",
  "name": "alert_catalog_auth",
  "label": "alert_catalog_auth Table",
  "description": "The alert catalog_auth table is part of the alert module and can be used for related query scenarios such as reporting.",
  "fields": [
    {
      "name": "catalog_id",
      "type": "bigint",
      "description": "The catalog association ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "auth_type",
      "type": "enum('user','team','role','common')",
      "description": "The auth_type field in the alertcatalog_auth table, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "auth_uuid",
      "type": "varchar(255)",
      "description": "Auth association UUID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "catalog_id",
        "auth_type",
        "auth_uuid"
      ],
      "description": "Primary key index covering the catalog_id, auth_type, auth_uuid fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
