{
  "moduleId": "alert",
  "name": "alert_team",
  "label": "AlertTeam Table",
  "description": "Alert team table, part of the alert module, used for related query scenarios such as reporting.",
  "fields": [
    {
      "name": "alert_id",
      "type": "bigint",
      "description": "Alert ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "team_uuid",
      "type": "char(32)",
      "description": "Group UUID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "alert_id",
        "team_uuid"
      ],
      "description": "Primary key index covering the alert_id, team_uuid fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
