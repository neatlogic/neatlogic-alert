{
  "moduleId": "alert",
  "name": "alert_view_auth",
  "label": "alert_view_auth Table",
  "description": "Alert view_auth table, related to the alert module data. Used for alert-related association queries and report queries.",
  "fields": [
    {
      "name": "view_id",
      "type": "bigint",
      "description": "The view's associated ID, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "auth_type",
      "type": "enum('user','team','role','common')",
      "description": "View the `auth_type` field in the `alertview_auth` table, used as the primary key or unique identifier for records in this table."
    },
    {
      "name": "auth_uuid",
      "type": "varchar(255)",
      "description": "Auth association UUID, used as the primary key or unique identifier for records in this table."
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "view_id",
        "auth_type",
        "auth_uuid"
      ],
      "description": "Primary key index covering the view_id, auth_type, auth_uuid fields, suitable for uniquely identifying records and queries that join the primary table."
    }
  ]
}
