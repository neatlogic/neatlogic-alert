{
  "moduleId": "alert",
  "name": "alert_catalog_auth",
  "label": "告警catalog_auth表",
  "description": "告警catalog_auth表，属于告警模块的数据表。 可用于告警相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "catalog_id",
      "type": "bigint",
      "description": "catalog关联ID，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_type",
      "type": "enum('user','team','role','common')",
      "description": "告警catalog_auth表的auth_type字段，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_uuid",
      "type": "varchar(255)",
      "description": "auth关联UUID，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "catalog_id",
        "auth_type",
        "auth_uuid"
      ],
      "description": "主键索引，覆盖 catalog_id、auth_type、auth_uuid 字段，适合用于唯一定位记录和主表关联查询。"
    }
  ]
}
